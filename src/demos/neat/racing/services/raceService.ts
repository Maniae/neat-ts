export class RaceService {

	init = () => {
		/**
		 * Called once at launch
		 */
	}

	update = () => {
		/**
		 * Called on every frame
		 */
	}

	onRaceEnded = () => {
		/**
		 * Called on every race timeout
		 */
	}
}
